name: particle_showcase
version: 1.0.0+1
publish_to: 'none'
description: Particle background showcase built from uploaded particle_background.dart

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
